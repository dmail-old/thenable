/*
voici des examples pour route

const input = true
pipe(
  input,
  (input) => Promise.resolve(null),
  (input) => Promise.resolve({ foo: input })
).then((output) => {
  // output is {foo : true}
})
*/
